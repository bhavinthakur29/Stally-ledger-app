/** Loaded in root `_layout` via `useFonts` — keys must match the map passed to `loadAsync`. */
export const CURRENCY_FONT_REGULAR = 'SpaceMono_400Regular';
export const CURRENCY_FONT_BOLD = 'SpaceMono_700Bold';
